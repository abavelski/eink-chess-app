1. revert the changes to the previous size of the board.

2. The inner line of the board border is a bit too thick, make it thinner.

3. Make the board accept fen notation as starting position

4. reset button should reset to fen position

5. in the saved file should be a list of fen positions, using kobo back and forward buttons should change the position on the board to the next and previous positions. Reset should still reset it

6. Make an example file, with 10 different random positions

7. commit and push

8. build and deploy if kobo is mounted
